1.var-let-const
function scope-block scope-block scope
re-declare:Yes-NO-No
re-assign:Yes-Yes-No
old style(avoid it)-value changes-valaue remains same


2.Spread Operator-It expands (spreads) elements of an array, object, or iterable into individual elements.

3.map-filter-foreach
map-transforms each item: returns new array use:when to change every item.
filter-keeps items that pass a test  use:for removing some item.
foreach-runs a task for each item  returns:nothing use :for several reasons.

4.An Arrow Function is a shorter way to write a function in JavaScript using the => syntax.
It makes code cleaner, shorter, and easier to read.

5.Template literals is a feature that help to write strings using backticks instead of single or double quates.
---


